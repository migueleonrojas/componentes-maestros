import { SecondsToTimeVideoPipe } from './seconds-to-time-video.pipe';

describe('SecondsToTimeVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToTimeVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
